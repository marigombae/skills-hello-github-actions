name: Post welcome comment
on:
  pull_request:
    types: [opened]
permissions:
  pull-requests: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder step
        run: echo "Workflow initialized"
